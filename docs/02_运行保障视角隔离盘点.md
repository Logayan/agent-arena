# 运行保障视角隔离盘点

**角色：** 谢临川｜平台运维与站点可靠性工程师  
**Run：** `run_6940048dd30f`  
**冻结证据：** `attempt-415cb8b318fda11f`，sequence `1..94`  
**隔离结论：** 映射可交付；完整迁移 `NO_GO`；OpenClaw 退役 `NOT_APPROVED`。

## 事实

- 冻结 `public_agent_safe` 投影含 94 条事件，sequence `1..94`。
- Registry 中 13 份生产源码及部署输入的原始字节、source event 和三阶段 Artifact 生命周期复核通过。
- 10 个入口的 Claude-only 路由探针通过，独立 OpenClawGateway policy probe 在 sequence 90 被拒绝。
- Runtime attestation 的 completed Session binding 为 0；当前无 Tool started/completed、Memory、暂停或恢复行为链。
- OpenClaw 历史基线文件只出现在 attestation 的 exclusion 名单，未提供原始字节。

## 运行保障异议

1. 启动时约 16.218 秒路由探针不能替代覆盖真实业务周期的零流量和单写者观测。
2. 源码定义 `worker.fencing.verified` 与 `worker.duplicate_side_effect.scan` 不等于旧 Worker 数据面写入真实被拒，也不等于副作用完成终态对账。
3. 暂停 API 的 Checkpoint 字段以完成节点、Artifact ID 和事件游标为主，仍需实测证明吸收在途 Tool、Session 和副作用状态。
4. 无恢复演练、回滚目标、RTO/RPO、容量和告警证据时，不批准生产退役。

## 建议公开合并议题

- 第一轮“事实与差异”：统一源码事实、事件行为事实和目标设计三种证据等级。
- 第二轮“残余风险与决定”：逐项给出负责人、故障注入条件、终态事件和 OpenClaw 回滚窗口。

## 证据边界

本隔离稿形成时，冻结事件投影没有 `agent.message.sent` 或通信轮次完成事件，因此其原始状态为：

```text
PENDING_PLATFORM_PUBLIC_COMMUNICATION
acceptance_eligible_as_completed_collaboration=false
```

节点随后收到四条公开 challenge/reply 并纳入最终综合；这些消息的内容级记录见 `collaboration/public-communication-record.json`。由于它们不在 sequence 94 冻结投影中，仍不得上调为冻结事件链内的协作 Runtime PASS。
