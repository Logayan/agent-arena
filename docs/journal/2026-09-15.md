# 2026-09-15 实施日志

## 最终 Judge 第 8 次裁决

- Run：`run_bda13e93b2ea`，Family：`run_0886dd109c15`，Version：9。
- sequence `162555`：最终 Judge 返回 `REVISE / 88`。
- sequence `162556`：旧执行器只将 `final_report_and_gap_list` 与 Judge 重置，进度 `90% → 80%`。
- sequence `162558`：正式库首次创建覆盖索引期间发生 SQLite 写锁冲突，Run 安全停止为 `failed / 80%`；原裁决、Attempt 与 Artifact 均保留。

## 本地实现

- 新增最终 Judge 全部上游责任节点路由及事实整改节点优先机制。
- 报告级退回自动补充最近的整改/返工/重跑节点，避免同一报告无限封版。
- 新增 Run 独立真实产品源码 checkout；工程执行、Judge、Commit 和远端交付使用冻结 Git 配置对应的真实目标分支。
- 保留历史 Run `code/` 审计仓库，不覆盖旧证据。
- 新增长 Run Artifact/Event 覆盖索引及最新退回事件独立投影。
- 页面解释 Judge 退回导致完成节点失效，不再把 90%→80% 表现为随机倒退。

## 验证

```text
定向索引测试：2 passed
路由/Git/执行安全合同：53 passed
后端全量：218 passed
Bridge：10 passed
前端 production build：1778 modules
生产 Runtime source attestation：passed
```

正式远端源码探针已确认目标仓库包含真实 `client/`、`server/` 与 Claude Agent SDK Bridge。

## 提交、恢复与真实 E2E

- 提交：`b7c338a fix: close runtime migration evidence loop`。
- Push：`origin/master` 已从 `6b9f95d` 更新到 `b7c338a`。
- 正式 8003 已在失败安全边界重启，Runtime 为 `claude_code / agent-sdk-bridge / 0.3.268 / Claude Code 2.1.268`。
- epoch44 的 `git.workspace.ready` 已指向 Run 独立 `product-source`，`target_commit=b7c338a32c53e181079b0c769561ce0aebf630be`，`authoritative_source=true`。
- 干预 `intervention_a732e449291d` 已在 sequence `162835` 应用到 `remediation_rerun`；sequence `162837/162838` 建立并启动新的不可变整改 Attempt。
- Claude SDK continuation 探针 sequence `162834` 通过，同一 platform session key 恢复为同一 SDK Session。

正式 Evidence Center E2E 于真实 `5173 + 8003` 完成 8/8 PASS，console/network/HTTP 错误均为 0。证据目录：

`deliverables/run_bda13e93b2ea/evidence-center-e2e/post-index-b7c338a-20260915-1536/`

产物包括 8 张截图、测试用例、逐项结果、JUnit、HTML 报告、截图索引和 SHA-256 Manifest。当前同一 Run 已进入真实产品源码上的 `remediation_rerun`，最终报告和独立 Judge 尚未完成，因此不提前宣称迁移通过。

## 90%→80% 与最终 Judge 循环诊断

- 第 8 次最终 Judge 已在 sequence `162555` 正常结束并返回 `REVISE / 88`，不是同一次裁决卡住。
- 百分比表示 10 个工作流节点的当前有效完成占比，不是 Judge 评分。旧路由重置报告与 Judge 两个节点，因此从 9/10 回到 8/10。
- Judge 的核心阻断为报告快照过期、唯一 package authority 缺失、semantic supersedes 错指 `.pyc`、测试/缺口分母冲突，以及 Tool、Memory、pause/recovery、exactly-once、十入口和完整产品测试证据未统一闭合。
- sequence `119020` 把 `max_revision_rounds` 设为 `0`，因此平台按发起人要求持续 Loop，不会因第 8 次退回自动终止。
- 旧路由只返工报告，无法改变底层事实，是稳定 `90→80→90` 的放大因素；`b7c338a` 已让报告级退回同时进入 `remediation_rerun` 和真实 `product-source`。
- 另发现干预重置 3 个节点后 Run progress 未立即重算，任务状态实际为 7/10 completed、页面仍显示 80%。代码已补充干预后的即时重算，前端改为动态展示当前待完成节点与“裁决已结束、正在返工”的准确语义。
- 进度修复验证：Runtime 安全合同 47/47、后端全量 219/219、前端 production build 1778 modules、Python 编译和 `git diff --check` 全部通过。一次禁用 pytest 插件造成的 3 个 anyio 用例失败已通过正常配置复跑排除。
- 完整分析归档：`docs/05-implementation/final-judge-revision-loop-root-cause-2026-09-15.md`。
- 正式整改后端测试出现 `55 failed / 163 passed`，抽样全部指向深层隔离目录触发的 Windows `WinError 206`；`SUBST` 被 `Path.resolve()` 展开，不能真正缩短。E2E 还出现 exit 0 但目标输出目录为空。已提交补充干预 `intervention_17e5080d9d3d`，要求改用系统 `TEMP/TMP` 的真实短目录执行，再将完整证据回收并注册，禁止把环境同源错误或无产物 exit 0 当作有效结论。
- 平台已新增当前系统自适应：Claude Runtime 仅在 Windows 且人物工作区路径达到 120 字符时切换到系统短临时根，并以 Run ID + execution-root 哈希保持隔离；非 Windows 和短 Windows 路径不变，可由 `JIANGHU_CLAUDE_RUN_WORKSPACE_ROOT` 显式配置。Runtime/Adapter 合同 32/32、后端全量 221/221、Python 编译与 `git diff --check` 通过。
- 正式 authoritative 后端重跑由 55 个长路径失败收敛为 1 个 SQLite URI 失败：baseline 脚本手工拼接 `file:` URI，在 Windows 短盘符/扩展路径下形成 invalid authority。宿主已改为 `Path.as_uri()`，baseline 定向 3/3 通过。
- sequence `166126` 暴露新的确定性假阻断：冻结快照公开投影 `162751`、private-audit 声明省略 `91`，两者已完整覆盖至 sequence `162842`；6540/6540 Artifact 原字节和生命周期也全部通过，但校验器同时要求 `metadata.event_count == projected_count` 与 `metadata.event_count == projected_count + omissions`，在 omissions 非零时逻辑上不可能同时成立，因此固定得到 `coherent=false / NO_GO`。同时 `run-metadata.event_count=162840` 比同一包实际 cutoff 落后 2，证明生成器混用了较早 Run 快照和稍后事件游标。
- 已向正式整改节点的谢临川登记 `correction`：`intervention_9d8960648635`，要求修复单一计数语义、同冻结边界生成、并增加 omissions 非零与并发事件增长回归，禁止通过放宽 Judge 绕过。
- 主工作区 `platform_executor.py` 已同步修复：事件快照返回 `source_event_count/cutoff_sequence`，`run-metadata.json` 的 `event_count/source_event_count/public_event_count/omitted_event_count/cutoff_sequence` 全部由同一冻结游标派生。新增合同测试通过；隔离测试库下 Runtime 安全合同为 `48 passed`。直接使用现场全局库的首轮收集被一个历史乱码路径 workspace 的 `PermissionError` 阻断，不属于本修改失败。
- 同一整改校验器还把 `run.completed` 设为裁决前非 `NO_GO` 的必要条件，但该事件只能在 Judge `ACCEPT` 后生成，形成“先完成才能裁决、先裁决才能完成”的循环死锁。已登记角色定向纠偏 `intervention_58a6b34a0295`，并补充节点级纠偏 `intervention_40d22bc758cd`，确保后续协调/集成角色必须拆分 pre-judge `READY_FOR_INDEPENDENT_JUDGE` 与 post-completion 终态验证，并新增前后阶段回归。
- 正式全量回归发现未知 `POST /api/*` 的状态码取决于 `client/dist` 是否存在：静态站点挂载存在时返回 405，源码环境返回 404。主工作区与正式隔离交付均已在静态挂载前增加 `/api/{unmatched_path:path}` fail-closed 404 边界，并把旧 Demo API 合同统一为 404；定向回归 `1 passed`。该修复避免前端构建时序改变 API 合同与全量测试结论。
- 20:02 实时复核：正式 API 显示 7/10 节点 completed、整改 running、报告和 Judge pending，因此有效进度为 70%，页面 80% 属于旧服务未加载即时重算修复。sequence 持续增长至 167557+，Claude SDK 心跳和工具事件正常，不是超时卡死。严格分析仍为 CAM 3/13；计数契约、pre/post Judge 解环和节点级综合纠偏三项 intervention 仍 queued，必须在当前 Attempt 安全边界应用后再生成新快照并进入 Judge，否则只会再次 REVISE。
- 20:31 完成 CAM-05 宿主修复：新增任务级 Artifact authority 原子替换、确定性集合 Manifest 哈希、`artifact.authoritative`/`artifact.authority.frozen` 原生事件以及应用内 authority/supersedes 回执展示。任务只在 Memory closure 与 authority 发布完成后标记 completed。验证为 Runtime 安全合同 53/53、后端全量 227/227、详情与返工集成 2/2、Bridge 10/10、前端构建 1778 modules；等待正式 Attempt 安全边界加载并产生现场事件。
- 22:27 定位到当前整改节点后置持久化的新增阻断：负责人业务输出已在 sequence `169762/169763` 完成，但 `.playwright-browsers` 下 310 个、约 453 MB 的浏览器运行时文件被误判为交付变更，从 sequence `169764` 起逐项写入 `agent.file.created`，现场数据库约 1.49 GB、WAL 约 131 MB，8003 健康接口因同步事件写入超时；最终 Judge 尚未调度。代码已统一排除该执行缓存，并把文件变更事件改为批量异步落库；等待当前旧进程到达安全 `paused` 后加载并定向重跑。
- 22:34 `py-spy` 证明 API 主线程阻塞在 `commit_run_changes` 的同步 `git add --all`；Git for Windows 启动器超时退出后，真实 `mingw64/bin/git.exe` PID 7936 成为孤儿并继续持有通信管道。为避免 453 MB 浏览器运行时被提交/推送，已只终止该孤儿 Git；平台保留现场并写入 sequence `174117 task.failed`、`174118 run.failed`，有效进度为 70%。新增修复会直接调用真实 Git、使用可配置 600 秒命令上限、将超时归类为 Git 交付错误，并把 Commit 移出 API 事件循环。
- 22:42 失败 checkout 共有 3,780 条变化，3,774 条为未跟踪内容，主要是历史 Evidence、交付包和测试临时目录。平台原逻辑错误地全树晋升人物 delivery，再执行无范围 `git add --all`。现已拆分为三条边界：本回合 Evidence 继续注册应用内 Artifact；只有本回合产品路径晋升到 checkout；Git 只暂存实际晋升路径。失败 checkout 的浏览器目录和 stale index lock 已移入 `.data/quarantine`，均可恢复。
- 修复验证达到：Runtime/Git/执行安全定向合同 85/85、后端全量 233/233、Claude Agent SDK Bridge 10/10、前端 production build 1778 modules、Python 编译与 `git diff --check` 全部通过。
