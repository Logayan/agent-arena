# 2026-09-13 工作日志

## 未完成重试时长连续递增

用户确认：Agent 在时限内尚未完成时，对应的下一次自动重试应提高时长，不能持续卡在 1800 秒。

执行器已按该语义完成补强：

- 仅 `timeout` 类错误提高下一次时长，网络、鉴权、配置和无效输出不盲目延长；
- 当前现场配置为基础 1800 秒、下一档 3600 秒、倍数 2、单回合上限 3600 秒；
- 人物级重试已经消耗的层级会传给节点级重试。若未来把上限提高到 7200 秒，时长按 1800 → 3600 → 7200 连续推进，不重复上一档；
- 总 Run 活动时长仍是独立硬边界，单回合放宽不能绕过总预算。

## 恢复 Worker 竞态修复

真实恢复中捕获到 Uvicorn startup 先于端口绑定的竞争窗口：两个后端进程可能在其中一个最终绑定失败之前，都启动同一 Run 的恢复。旧代码只有 `worker.lease.acquired` 审计文字，没有真实互斥，最终导致 epoch 并发、SQLite `IntegrityError` 和错误的 `run.failed`。

现已增加进程生命周期单写租约：SQLite 使用 OS 文件锁，PostgreSQL 使用 advisory lock。竞争实例在任何 Run、Task 和事件写入前退出；进程退出后锁由操作系统或数据库连接自动释放。定向合同测试证明同一 Run 同时只能有一个 Worker，释放后可立即接管。

## Run 家族活跃版本展示

第 9 版正在执行期间，两个误创建的较高版本已取消；旧列表逻辑按最高版本盲选，导致事件入口把已取消版本显示为“当前”，掩盖正在运行的第 9 版。`PlatformStore.list_runs()` 现优先返回家族中的 `running`、`pause_requested` 或 `paused` 版本；没有活跃版本时再返回最高版本。测试覆盖“第 2 版运行、第 3 版取消”的家族选择场景。

## 验证与现场

- 后端全量：133 passed；
- Runtime 执行安全合同：21 passed；
- 重试与恢复定向场景：2 passed；
- Claude Agent SDK Bridge：6/6 passed；
- 前端生产构建：1777 modules；
- Python compile：通过；
- `git diff --check`：通过，仅有 Windows 行尾提示；
- 凭据模式扫描未发现真实 Key 落盘，两个命中文件均为 `risk-management-framework` 字符串假阳性。

真实前端已切换到第 9 版 `run_bda13e93b2ea` 复核，页面显示 2 名独立审计人物正在执行命令，当前节点为“运行证据独立质量与风险审计”，没有展示 Provider URL、Session、工具策略或 transport trace。第 10 版 `run_9e55093dad40` 和第 11 版 `run_cd27b80a7451` 已取消，原始创建与取消证据保留。迁移仍需获得最终 `gate.passed` 和 `run.completed` 后才能宣布完成。
## 00:18 未完成回合的完整超时递增链

复核发现，自适应超时代码虽然已经把人物级消耗档位传给节点级重试，但默认和当前 Worker 启动参数的上限仍是 3600 秒。以基础 1800 秒、两次人物尝试和两次节点尝试计算，实际会变成 `1800 → 3600 → 3600 → 3600`，与“后续未完成重试继续增加时长”的目标不完全一致。

已将默认单回合上限调整为代码安全硬上限 14400 秒，目标链路为 `1800 → 3600 → 7200 → 14400`。总 Run 活动时间预算仍是更外层硬边界；只有 `timeout` 分类会提高下一档，网络、鉴权、配置和无效输出不会靠无限延时掩盖。第 9 版当前 Worker 在 00:13:46 启动时已经继承 3600 秒旧上限，运行中的进程无法热改，因此先保留两名审计人物正在写入的真实产物，待安全恢复边界重启并以 14400 秒上限加载，不新建 Run 版本。

首次完整链测试实际得到 `1800 → 3600 → 7200 → 10800`，定位为测试 Workflow 自身的 180 分钟总 Run 时限主动裁剪第 4 档，并非部署级 `JIANGHU_MAX_RUN_MINUTES` 或超时层级重置。将该测试 Workflow 的策略时限显式设为与真实验收一致的 720 分钟后，预期链为 `1800 → 3600 → 7200 → 14400`；保留总预算裁剪作为产品安全约束。

定向合同最终 `22 passed`，完整后端 `133 passed`。并行生产构建发现 `client/src/App.vue` 残留一套已被“凭据库 + 项目仓库”替代、且模板不可达的旧 Git 全局交付表单函数，导致 `testGitDelivery`、`saveGitDelivery` 被 TypeScript 判为未使用。已移除这组不可达前端状态和加载请求，不修改现有 Git 凭据、项目仓库、事件级交付与后端兼容 API。

前端生产构建整改后通过：Vite 转换 1777 个模块。Claude Agent SDK Bridge 6/6、Python compile、`git diff --check` 同步通过。

00:30 对第 9 版发出受控暂停请求，平台登记 `run.pause_requested`、Checkpoint Artifact、采集与下载字节复算、`run.checkpoint.persisted`。已发出的两名审计人物先完成当前回合，其中质量工程师在 sequence 2888/2889 提交真实 Claude SDK 回合与独立审计产物；随后 sequence 2907 进入 `run.paused`。

安全边界上停止旧 Worker，并以以下非敏感运行参数重启同一个本地后端：

```text
JIANGHU_AGENT_RUNTIME=claude_code
JIANGHU_AGENT_TIMEOUT_SECONDS=1800
JIANGHU_AGENT_TIMEOUT_MAX_SECONDS=14400
JIANGHU_AGENT_TIMEOUT_RETRY_MULTIPLIER=2
JIANGHU_CLAUDE_MAX_TURNS=100
JIANGHU_MAX_RUN_MINUTES=720
```

新进程 PID 28680，8003 唯一监听；Runtime 健康为 `claude_code / agent-sdk-bridge / SDK 0.3.268 / Claude Code 2.1.268`。Git 凭据与项目仓库接口均返回 200。sequence 2908/2909 恢复原 Run 和 Checkpoint，sequence 2910～2919 建立 execution epoch 11、真实单写 lease 和 fencing，不创建新 Run 版本。

浏览器刷新 `http://127.0.0.1:5173/?realm=org_jianghu` 后已真实复验：总览恢复 3 个组织、17 位人物、9 条章法；事件列表优先显示“当前第 9 版 正在执行”；事件现场显示 2 名审计人物在办事；模型与凭据页显示 Runtime 正常、模型 `gpt-5.6-sol`、凭据仅掩码，并能加载 1 条 Git 凭据和 1 个项目仓库。刷新后的请求不再出现旧路由 404 或 Bad Gateway。

进一步确认通过 Codex 临时终端直接前台运行 Uvicorn 时，终端会话约 10 分钟后被宿主回收，进而中断后端和 Bridge；这不是 Run 自身超时。已将 `scripts/start-real-api.ps1` 从旧 Anthropic 环境变量启动方式迁移为当前 Claude Code Runtime 后台启动器：默认端口 8003、`1800/14400/2` 自适应超时、100 turns、720 分钟总上限，使用 `Start-Process -WindowStyle Hidden`，并把 stdout/stderr 固定写入 `.data/logs/`。Provider 地址、模型和 Token 仍只从平台数据库读取，启动脚本不包含凭据。

sequence 3660/3661 给出反例：一个历史后台启动器未注入时长环境变量时，两名真实审计人物仍记录 `timeout_seconds=600`、`next_timeout_seconds=1200`。执行器当时只用 `is_engineering or is_judge` 选择长任务默认值，没有依据 `actor_has_tools`，导致使用 Read/Write/Bash 的独立质量与风险审计错误落入纯文本 600 秒档。已修为任何拥有 Runtime 工具的人物默认基础 1800 秒，并让当前/下次时长计算使用同一判定；目标完整链保持 `1800 → 3600 → 7200 → 14400`。

## 05:14 超时升档与暂停边界续跑

用户再次确认：如果 Agent 仍未完成，下一次重试应增加时长，不应固定卡在 1800 秒。当前实现已经满足：只有 `timeout` 分类消费下一档，人物级与节点级共享连续层级，完整链为 `1800 → 3600 → 7200 → 14400`；非超时失败不扩时。真实事件已证明 1800 秒超时后升到 3600 秒并完成，历史非超时异常保持 1800 秒。

静态复核刚加入的 Artifact、恢复、Tool、Memory 和 Runtime 证据代码时，发现 Git 交付配置复制片段误落进文件 Artifact 注册方法，存在引用未定义 `retry_id` 的风险；已将其移回定向重试 Run 的事务。另让隔离 delivery 根目录兼容非工程人物文件归档，并确保 Session continuation 证据探针失败只形成负面证据、不阻断用户工作流或消费测试替身的真实节点回合。

验证结果更新为：后端及 Runtime 合同 `136 passed`，Claude Agent SDK Bridge `6/6 passed`，前端生产构建通过（1777 modules），Python compile 通过。第 9 版 `run_bda13e93b2ea` 仍处于 `pause_requested`，最终报告团队已放行的综合回合持续产生工具、文件和测试事件；平台未卡死。继续等待 `run.paused` 安全边界后再重启加载新代码，不强杀、不创建新 Run 版本。

## 05:49 安全重启与第三轮系统架构封版

第二轮最终报告综合回合在 sequence 11312 到达 1800 秒边界，平台真实记录 `timeout_seconds=1800`、`next_timeout_seconds=3600`、`timeout_extended=true`，随后 3600 秒回合继续执行并完成。sequence 11705 进入 `run.paused`，进度 90%；确认 8003 只有 PID 14792 监听且数据库状态确为 paused 后，受控停止该进程，未强杀正在运行的 Agent 回合。

暂停期间保持同一 `run_bda13e93b2ea`、同一 family 和第 9 版，将 `final_report_and_gap_list` 改为系统架构师 `agent_cb495a9bea1e` 牵头，产品负责人 `agent_708110b6a225` 与质量工程师 `agent_445e5f7e8189` 共同参与。配置前后值已登记为不可变 Artifact `artifact_cd4eb2f7d7c1` 并完成 SHA-256 复读；第二轮产物作为历史证据保留，节点重新置为 pending，不创建新 Run 版本。

新后端使用 `scripts/start-real-api.ps1` 的 `1800/14400/2`、100 turns、720 分钟配置启动；实际 8003 监听 PID 为 29292。Runtime 健康实测为 `claude_code / agent-sdk-bridge / SDK 0.3.268 / Claude Code 2.1.268`。恢复请求的 HTTP 客户端因超大 Run 投影在 30 秒超时，但服务端已成功处理，数据库 sequence 11710～11715 显示 `run.resumed`、Checkpoint 加载、`run.recovered`、`agent.runtime.recovered` 和 Git 工作区恢复，状态为 running、版本仍为 9。

新恢复链已形成以下真实证据：9 个入口均动态解析到 Claude Code SDK；`runtime.route.attested` 记录 OpenClaw traffic 0、dual-write 0；fallback deny 与 rollback exercise 均 passed；旧 lease revoked、stale writer deny passed；历史 1 个重复 Tool 终态已确定 canonical，144 个失败终态逐项 review，恢复后 duplicate side effect count 为 0；SDK continuation 两回合返回同一个真实 Session ID。sequence 11883 的第三轮 `task.started` 由系统架构师牵头，三位配置成员均有独立 `team.member.started` 事件。

## 09:06 未完成重试扩时与同版本预算续跑

再次确认最终语义：回合未完成且错误分类为 `timeout` 时，下一次人物级或节点级自动重试按 `1800 → 3600 → 7200 → 14400` 增加时长；已完成成员不重跑，非超时错误不扩时。真实事件 14605（系统架构师）和 16112（最终 Judge）均记录 `timeout_seconds=1800`、`next_timeout_seconds=3600`、`timeout_extended=true`，第二次尝试实际越过原 1800 秒边界继续执行。

最终 Judge 的合法 `verdict.json` 已可从同一隔离 Claude SDK delivery 恢复，真实裁决 `REVISE` 正确定向退回 `final_report_and_gap_list`。返工启动后 sequence 17396 命中的是 Run 总活动预算 420 分钟，而不是 Agent 1800 秒超时。已通过原 Run 的扩时接口增加 120 分钟：sequence 17397、有效预算 540 分钟、最大 720 分钟、版本仍为 9。随后 sequence 已增长到至少 17633，三位最终报告成员持续产生 Read、Bash、Schema 校验、授权和副作用核验事件；当前节点 running，最终 Judge pending，现场未卡死。

## 11:00 最终 Judge 缺口与节点边界暂停

第四轮最终 Judge 返回 `revise / 77`，剩余问题集中在机器证据而非报告篇幅：五角色参与不足、seal/reveal/decision/freeze 缺失、Tool disposition 与 canonical 字段被公开投影过滤、Memory 最新提交缺少新 SDK Session 闭环、retry 入口和动态零流量计数缺失、同 Run Attempt 血缘错误指向父 Run。

已对原第 9 版 Run 发出安全暂停，sequence 21615 持久化请求 Checkpoint；等待期间事件继续增长至至少 22596，成员持续执行命令和测试，因此判定为自然排空而非卡死。没有停止进程、没有强杀人物回合。

代码已补齐公开证据字段、retry 路由、旧 lease 身份、同 Run Attempt 血缘、多人贡献封存与揭示授权、正式决定与线程冻结、Artifact superseded 事件以及生产 Claude SDK Memory commit 后新 Session 闭环。执行期策略改为通过当前 Run 的不可变 amendment 事件加载，不修改 Workflow/Run 版本。验证结果：执行安全合同 29 passed；后端全量 147 passed；Claude Agent SDK Bridge 6/6；前端生产构建通过；compile 和 diff check 通过。待 `run.paused` 后登记五角色与 `max_revision_rounds=6` 的同版本 amendment、请求定向返工、受控重启并 resume。

## 12:27 五角色同版本返工与启动恢复补漏

sequence 26150 已进入真实 `run.paused`。在原 `run_bda13e93b2ea` 第 9 版内追加五角色执行策略、`max_revision_rounds=6`、定向 `require_rework` 和 `run.restart.requested`，随后于安全边界受控停止旧后端 PID 30676。第一次新后端虽正常监听，但启动恢复未生成接管事件；定位到 `list_runs_by_status()` 只返回 Run 状态行，恢复函数却从该行读取不存在的 events/tasks。

已修为 paused Run 启动时使用 `event_limit=200` 且不加载 Artifact 正文的轻量详情投影，并新增回归测试，结果 1 passed。第二次受控重启后，真实 sequence 26153/26154 分别产生 `run.interrupted`、`run.restart.completed`。resume 返回 HTTP 200；intervention 已 applied，sequence 26252 创建返工 loop。sequence 26267、26280、26293、26305、26318 证明产品负责人、工程师、系统架构师、安全风险审计、QA 五个指定角色均已进入独立 Claude Code SDK 执行回合，Run/版本和历史产物未被覆盖。

五角色首轮 timeout 在 sequence 28214～28218 同批登记，全部由 1800 秒升到 3600 秒；第二回合继续执行约一小时并产生大量真实 Read/Edit/Bash/测试事件。sequence 31356 进一步证明 3600 秒仍未完成时，平台计算 `next_timeout_seconds=7200`、`timeout_retry_level=1`、`timeout_extended=true`，sequence 31357 进入节点整体重试。现有线程心跳已从旧 Version 8 Run 更新为只监控和推进 `run_bda13e93b2ea` Version 9，每 5 分钟检查一次，状态健康时保持安静，仅在最终完成、不可恢复失败或需要权限时通知。

## 15:21 测试产物与页面证据硬门禁

用户指出当前输出产物没有完整覆盖测试用例、测试截图和逐项测试情况。对真实第 9 版 Run 工作区执行只读盘点后确认该意见成立：`run_bda13e93b2ea` 的隔离工作区内 PNG/JPG/HTML/HAR/视频媒体证据数量均为 0，仅存在若干 `test-results.log`，没有独立结构化测试用例清单和逐项结果文件。平台事件中的 `agent.test.completed` 只能证明某个命令被归类为测试并结束，不能替代真实测试报告、页面证据和可复现步骤，因此当前证据不足以支持最终 ACCEPT。

已通过正式平台接口向当前 `final_report_and_gap_list` 节点（task `task_764f124e78e1`）提交 `require_rework`，intervention 为 `intervention_9f1b3ba6c5e8`，sequence 34385 记录 `user.intervention.queued`。当前 7200 秒回合继续自然排空；回合结束后由平台在同一 Run、同一版本内返工该节点并重跑下游 Judge，不覆盖历史产物。

返工后的权威证据包新增以下硬门禁：

- `test-cases.json` 与可读测试用例文档，逐项记录 case ID、OpenClaw 能力基线映射、前置条件、真实步骤、预期/实际结果、状态、严重度、命令、时间、事件与 Artifact；
- `test-results.json`，明确 PASS/FAIL/BLOCKED/SKIPPED 和通过率，BLOCKED/SKIPPED 不得计入通过；
- 使用当前 5173 前端、8003 后端和本次 Run 的 Playwright 真实浏览器 E2E，不接受静态 HTML、Mock 页面或历史 Run；
- 每个关键场景至少一张 PNG，失败场景保存整改前后截图，并由 `screenshot-index.json` 绑定 case ID；
- 全部测试报告、截图、日志和索引进入 Artifact Registry、证据索引、SHA-256 Manifest 和最终 ZIP；缺少任一硬门禁时最终 Judge 必须 REVISE。

平台展示层同步发现并修复两个会削弱可复核性的缺陷：截图虽可按原始字节登记，但旧实现把 PNG 等媒体统一标成 `application/octet-stream`，Artifact 下载名也被强制追加 `.md`，前端只能显示文件回执而不能直接预览。现已增加确定性 MIME 映射、保留原扩展名的下载响应和 `inline=true` 预览；前端 Artifact 区新增“测试证据完整性”卡片，分别统计测试用例、逐项结果、页面截图、浏览器报告/索引，缺一项即显示“仍有缺口”，图片 Artifact 可在页面内展开预览。

代码验证为：截图存储与接口响应定向回归 `4 passed`，Python compile 通过，前端生产构建通过（1777 modules），`git diff --check` 通过。真实 Run 随后在 sequence 35476 记录 `user.intervention.applied`，团队公开议事和新回合已加载该要求；最终 Judge 仍不得在四类测试证据齐备前 ACCEPT。

真实浏览器打开当前 5173 页面时进一步发现页面长期停留在“正在读取江湖状态”，`/api/health` 与组织接口均超过 15 秒。8003 仍由同一 Python Worker 监听，Run 事件同时从 35498 增长到 35591，说明 Agent 执行没有死亡，阻塞点是 HTTP 主事件循环。根因是介入接口在异步 handler 内同步加载并公开投影整个三万余事件 Run；客户端超时不会立即停止服务端 CPU 密集投影，因而健康检查和前端读取也被拖住。

介入接口现改为只查询轻量 `run_state` 和 task summary，不再加载全部 events、artifacts、dossiers，也不再向前端返回完整 Run。前端在现有 Run 上局部合并 intervention 与状态后恢复轮询，避免丢失当前页面上下文。新增接口合同明确让 `get_run()` 在测试中主动失败，以证明介入路径没有偷偷退回全量投影；连同截图链路共 `5 passed`，前端生产构建通过。运行中的旧 Worker 不热重载该修复，待当前回合安全结束后再加载。

## 16:14 长周期 Run 取消固定总时长与轻量轮询

用户确认真实 Agent 任务可能持续数天，固定 Run 总分钟数不应作为默认硬终止条件。执行策略改为固定总时长默认关闭：仅当 Workflow 明确声明 `enforce_run_time_limit=true` 时才应用 `max_run_minutes`；默认继续保留单次 Claude SDK 人物回合超时、`1800 → 3600 → 7200 → 14400` 自适应重试、Token/成本预算、人工取消、失败终态和心跳/停滞观测。总历史活动耗时仍保留用于审计，不再阻止新的恢复 epoch。

唯一验收 Run `run_bda13e93b2ea` 在 sequence 35729 被旧累计时限判为 `budget_exhausted`，没有覆盖历史证据。旧时限恢复入口现允许同 Run、同版本原地恢复；只重跑 `final_report_and_gap_list` 与下游 `final_independent_judgement`。恢复后事件增长到至少 35841，最终报告节点重新 running，证明 Claude Code SDK Worker 已继续真实行动。

同时确认前端对大 Run 的完整轮询会重复投影三万余事件并放大 SQLite 锁竞争。新增 `/api/platform/runs/{run_id}/state` 轻量状态接口，仅返回 Run 状态、任务摘要、最近事件、Artifact 数量和四类测试证据计数；运行中前端只合并轻量快照，终态后才读取一次完整卷宗。该接口不读取 Artifact 正文、不构建节点 dossier，也不覆盖历史事件。

## 19:40 Claude SDK 人物回合改为心跳续期

`run_bda13e93b2ea` 的 epoch29 loop1 在持续执行的情况下仍于 sequence 49157 命中 1800 秒 `model_request_timeout`。平台正确保留现场并创建 `epoch29:loop2:node2`，下一窗口为 3600 秒，Attempt 具备 `rework_of`、`supersedes` 和 causation；但该事实说明人物回合仍有总墙钟限制，与“正常执行的任务可以持续数天”不一致。

代码已改为 heartbeat 驱动的停滞检测：Bridge 周期输出内部 heartbeat，Runtime 以“连续无输出时长”而非“累计运行时长”判定 timeout。健康回合每次 heartbeat、模型消息或 Tool 事件都会续期，没有总执行时长上限；只有 Bridge 不再响应时才产生 `claude_stalled` 并清理进程树。Tool 命令超时、人工取消、Provider 错误、预算和失败分类仍保留。

定向回归：Python Runtime 合同 14 passed，Node Bridge 7/7 passed，Python compile 通过。当前 loop2 已真实启动五个 Claude Code SDK Bridge 子进程并持续产生工具事件；运行中的后端不热加载新语义，待安全边界后重启。

## 21:13 失败边界加载心跳修复并原 Run 恢复

`final_report_and_gap_list` 的 epoch29 loop2 持续产生工具与测试事件至 sequence 55852；工程角色最后一项预检实际以退出码 0 完成。随后旧后端仍在 sequence 55853 将人物回合判为自动重试后未完成，并于 sequence 55854/55855 写入 `task.failed`、`run.failed`。最终 Judge 保持 pending，没有伪造 ACCEPT、Gate 或 Run 完成事件。

失败终态构成可安全重启边界。核对 8003 唯一监听 PID 34528 且无子进程后，受控停止旧后端；5173 前端 PID 26864 保持运行。随后使用 `scripts/start-real-api.ps1` 启动已提交代码，参数为 Claude Runtime、基础停滞窗口 1800 秒、最大 14400 秒、倍率 2、100 turns。新 Runtime 健康实测为 `claude_code / agent-sdk-bridge / SDK 0.3.268 / Claude Code 2.1.268`，模型 `gpt-5.6-sol` 与加密凭据均由平台数据库正常加载；未在命令或日志中输出 Token。

通过 `POST /api/platform/runs/run_bda13e93b2ea/recover` 从 task `task_764f124e78e1` 定向恢复。响应确认：Run ID 与 Version 9 不变；只恢复 `final_report_and_gap_list` 和下游 `final_independent_judgement`；其余八个已完成节点与全部旧事件、失败证据和 Artifact 保留。sequence 55877～55888 完成真实 lease 排他探针、旧 Worker fencing、Checkpoint 载入以及中断 Tool/Command 的 interrupted 终态封口，没有把未知结果冒充成功。

恢复 execution epoch 随后启动五个独立 Claude Code SDK Bridge 进程。sequence 从 56062 持续增长到至少 59206，产品、架构、工程、QA、安全角色均产生真实 Read/Write/Edit/Bash 与测试事件，多项命令和测试返回成功；截至本次记录尚无 `agent.turn.completed`、`task.completed`、Judge、Gate 或 `run.completed`，因此 Run 仍为 running、进度 80%，继续监控而不提前宣告迁移通过。

## 22:10 固定 100 Turns 隐藏中断修复

epoch30 中系统架构师、产品负责人、工程角色先后完成独立贡献；QA 与安全角色在持续产生大量工具事件后分别于 sequence 60679、60674 进入人物级重试。两者错误分类均为 `runtime_failure` 而非 timeout，因此未错误消费时长扩容档位。

读取 Claude Code 自己的持久 Session 终态附件后得到确定证据：QA Session `02ba4382-f8f2-49df-90c3-9335037a3376`、安全 Session `9954a43b-88c4-4cab-9658-2f77d59f0513` 均为 `max_turns_reached`，配置 100、实际 101；QA 历史 Session `80563164-e52f-47a8-9b03-04b0d751bde5` 也有相同终态。根因是启动器仍设置固定 `JIANGHU_CLAUDE_MAX_TURNS=100`，健康长回合虽不再受墙钟总时长限制，却仍被第 101 turn 隐式截断。

现改为 SDK `maxTurns` 默认不下发，只有部署显式配置正整数时启用；启动器默认 0 并清除旧环境变量。Token/成本预算、命令超时、取消和 heartbeat 停滞检测保持不变。定向验证为 Python Runtime 合同 48 passed、Node Bridge 8/8 passed、compile 通过。运行中的两次人物重试继续自然执行，待安全边界后重启加载该修复，不强杀当前回合。

安全角色第二次回合于 sequence 62590 再次因旧 100-turn 配置失败，62591 将节点置为整体重试；此时所有旧 Bridge 已退出。暂停请求的 Checkpoint 于 sequence 62608 持久化，受控重启后的启动器返回 `ClaudeMaxTurns=null`，Run 自动收敛为 paused。resume 后保持原 Run 与 Version 9，sequence 62763 创建 epoch31 Attempt；五个不带默认 Turn 上限的新 Claude SDK Bridge 于 23:07～23:08 全部启动，事件增长到至少 62986。

## 23:35 execution epoch 恢复 Attempt 血缘修复

复核 epoch31 的 `attempt.created` sequence 62763 时确认：新 epoch 把 `loop_round` 重置为 1 后，旧逻辑错误写入父 Run `run_9226059d74a1` 作为 `rework_of_run_id`，同时 `rework_of` 为空。该 Attempt 实际属于 `run_bda13e93b2ea` 的同 Run、同 Version 恢复，血缘与事实不符。

实现改为优先使用同节点最近前序 Attempt 的实际 `run_id` 和 `platform_attempt_id`，不再把 `loop_round > 1` 当作同 Run 返工的必要条件；真正没有当前 Run 前序 Attempt 的跨 Run 首次重试仍保留父 Run 关系。合同测试新增“新 epoch、loop1、存在同 Run 前序 Attempt”场景，结果为 `34 passed`，Python compile 与 diff check 通过。本机全局 pytest 的 `logfire/opentelemetry` 自动插件存在版本冲突，验证时禁用无关插件并显式加载 AnyIO，不把该环境问题计为产品失败。

当前真实 Run 仍为 running、80%，事件已增长到至少 sequence 65353，五角色继续产生真实工具与文件证据。既有 sequence 62763 作为不可变历史证据保留；不在 Bridge 活跃期间重启后端，修复将在下一个安全边界加载。

血缘修复提交 `b137cdb` 已推送至 `origin/master`。随后以项目虚拟环境和隔离 pytest 插件集执行后端全量回归，结果 `178 passed, 1 third-party warning`；Claude Agent SDK Bridge `8/8 passed`；前端 production build 通过并转换 1777 modules。真实 Run 在回归期间继续从 sequence 65353 增长到至少 65987，证明工程测试未阻断平台 Worker。轻量状态接口仍显示测试证据 `cases=4`、`results=4`、`screenshots=18`、`browser_reports=8`，但 `case_documents=0`、`junit_reports=0`、`complete=false`，因此当前不能把已有截图和零散结果误判为测试交付门禁已闭合。

补充安全与生产路由核验：`scripts/check-no-secrets.ps1` 返回 `secret_scan=passed`，Runtime Port 与 source-attestation 定向合同 `44 passed, 1 third-party warning`。epoch31 最近 300 条事件覆盖产品、架构、工程、QA、安全五角色；产品负责人在 sequence 66208 完成人物回合、sequence 66218 提交独立贡献，另外四个 Node Bridge 与 Claude 子进程仍由后端 PID 34808 持有并继续产生工具/测试事件。该进程树和事件增长是当前等待的权威活跃证据，不因短暂无公开事件而重启。
