[project]
name = "jianghu-openclaw-runtime-mapping"
version = "2.0.0"
description = "Evidence-closed OpenClaw to Claude Agent SDK Runtime boundary verifier"
requires-python = ">=3.11"

[tool.unittest]
start-directory = "tests"
